# Social Media Platform (Scaffold)

This folder contains a static, client-side scaffold for a simple social media demo. Files are intentionally minimal and use LocalStorage for persistence.

Open `index.html` to start (redirects to `login.html` if not signed in).
